interface:
  display_name: "github-pr-ops"
  short_description: "Use when creating or updating GitHub pull requests, comments, reviewer requests, and handoff notes."
policy:
  allow_implicit_invocation: true
