# `.prototype` placeholder

Place your visual source of truth here.

This can include local route prototypes, static HTML/CSS references, exported screens, or other design-source material that the repository treats as the canonical UI reference.

Rules:

- do not edit `.prototype` during routine implementation work
- compare UI changes against this directory
- document any intentional divergence in evidence and PR notes
