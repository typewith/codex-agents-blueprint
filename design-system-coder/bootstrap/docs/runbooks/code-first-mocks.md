# Runbook: code-first mocks

## Workflow

- use `scripts/prototype/generate-mock.mjs` to scaffold review notes
- implement the thinnest mock that still covers the important states
- prefer real navigation structure over a static screenshot shell
- record assumptions before the PR is opened
