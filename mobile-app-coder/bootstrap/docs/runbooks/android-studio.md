# Runbook: Android Studio

## Checklist

- confirm Android SDK and emulator assumptions
- capture signing, native dependency, and build caveats
- use `scripts/mobile/run-native-tooling-check.mjs --platform android` to create a review artifact
- keep human validation steps explicit
