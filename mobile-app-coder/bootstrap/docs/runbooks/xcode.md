# Runbook: Xcode

## Checklist

- confirm simulator, provisioning, and signing assumptions
- capture native dependency caveats
- use `scripts/mobile/run-native-tooling-check.mjs --platform ios` to create a review artifact
- keep human validation steps explicit
